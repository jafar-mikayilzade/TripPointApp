/** Shared auth deep-link constants (no imports — breaks require cycles). */

/** Supabase email confirm / magic link sonrası app-ə qayıdış. */
export const AUTH_CALLBACK_URL = 'trippoint://auth/callback';
